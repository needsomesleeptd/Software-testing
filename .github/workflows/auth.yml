name: Go E2E BDD tests

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.23.1'

      - name: Run E2E tests
        env:
          USER_EMAIL: ${{ secrets.LAB_04_USER_EMAIL}}
          USER_PASSWORD: ${{ secrets.LAB_04_USER_PASSWORD }}
          USER_SECRET: ${{secrets.LAB_04_USER_SECRET}}
        run: make run-e2e

      - name: Remove env
        env:
          USER_EMAIL: ${{ secrets.LAB_04_USER_EMAIL}}
          USER_PASSWORD: ${{ secrets.LAB_04_USER_PASSWORD }}
          USER_SECRET: ${{secrets.LAB_04_USER_SECRET}}
        run: make docker-rm